# ============================================================
#  Modern Data Stack — dépendances locales (venv)
#  Les sources (Postgres / MinIO) tournent dans Docker ;
#  tout ce qui suit s'exécute en local dans le venv.
# ============================================================

# --- Data Warehouse local ---
duckdb==1.1.3

# --- Transformation (T) ---
dbt-core==1.8.7
dbt-duckdb==1.8.4        # 1.9+ requiert dbt-core 1.9 (MicrobatchConcurrency)

# --- Orchestration ---
prefect==3.1.5           # 3.7.x bug FastAPI routing ; 3.0.x bug Pydantic 2.13
fastapi==0.111.1         # 0.115+ casse PrefectRouter.routes
pydantic>=2.7.0,<2.10   # 2.13 casse ResultRecordMetadata dans Prefect 3.1.x

# --- Ingestion / connecteurs sources ---
psycopg2-binary==2.9.10      # extraction PostgreSQL (localhost:5433)
minio==7.2.10                # extraction MinIO       (localhost:9000)
boto3==1.35.71               # alternative S3-compatible pour MinIO (optionnel)
pandas==2.2.3                # manipulation des DataFrames lors de l'EL
pyarrow==18.0.0              # passerelle Arrow Pandas <-> DuckDB (perf)
numpy

# --- Visualisation ---
streamlit==1.40.1

# --- Qualité de la donnée ---
soda-core
soda-core-duckdb==3.3.20     # connecteur Soda pour DuckDB
sqlalchemy


# --- Configuration / environnement ---
python-dotenv==1.0.1         # chargement des secrets depuis .env

# --- Qualité de code (CI/CD) ---
ruff==0.8.2
